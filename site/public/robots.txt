# Record Atlas — everything here is meant to be found.
#
# The gazetteer shards under /g/ are a search corpus fetched three letters at a
# time: 6,263 machine-readable fragments with no prose in them. They are not
# worth a crawl budget, and neither are the per-place JSON files, whose content
# is already on a readable page under /place/.

User-agent: *
Allow: /
Disallow: /g/
Disallow: /p/

Sitemap: https://daviddef.github.io/TheMap/sitemap.xml
